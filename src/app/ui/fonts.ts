// Google Fonts
import {Open_Sans, League_Gothic } from 'next/font/google';

export const open_sans = Open_Sans({ subsets: ['latin'] });
export const league_gothic = League_Gothic({ 
    weight: ['400'],
    subsets: ['latin'],
});